---
exclude_paths:
  - .cache/
  - .github/
  - test

enable_list:
  - empty-string-compare # opt-in
  - no-log-password # opt-in
  - no-same-owner # opt-in

skip_list:
  - role-name[path]
  - galaxy[no-changelog]
